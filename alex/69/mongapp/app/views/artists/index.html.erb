<p id="notice"><%= notice %></p>

<h1 style="color:blue">Artists</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @artists.each do |artist| %>
      <tr>
        <td><%= artist.name %></td>
        <td><%= artist.area %></td>
        <!--<td><%= link_to 'Show', @artist %><td> -->
        <td style="padding: 2%"><a href="artists/show?name=<%=  artist.name %>">show</a></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<div style="float:right"><%= link_to 'New Artist', new_artist_path %></div>